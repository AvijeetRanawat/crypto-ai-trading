[pytest]
addopts = -q
asyncio_mode = auto
testpaths = tests test_profit_rebuild.py
python_files = test_*.py
markers =
    unit: fast isolated unit tests
    integration: cross-module tests with db/api interaction
    smoke: broad health checks across critical routes
